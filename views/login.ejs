<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ParkScout - Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background-color: rgb(0, 123, 255);
    }

    /* navbar section */
    .custom-navbar .nav-item {
      display: flex;
      flex-direction: column;
      align-items: center;
      margin: 0 15px;
    }

    .custom-navbar .nav-link {
      display: flex;
      flex-direction: column;
      align-items: center;
      color: white;
      /* Customize color */
      text-decoration: none;
      font-size: 14px;
      padding: 10px 5px;
    }

    .custom-navbar .nav-icon {
      font-size: 20px;
      margin-bottom: 5px;
    }

    .custom-navbar .nav-link:hover {
      color: rgb(13, 110, 253);
    }

    .login-form {
      margin: 50px auto;
      max-width: 450px;
      padding: 30px;
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      animation: fadeIn 1s ease-in-out;
    }

    .login-form h2 {
      font-size: 2rem;
      color: rgb(0, 123, 255);
      font-weight: bold;
      margin-bottom: 20px;
    }

    .btn-custom {
      background-color: rgb(0, 123, 255);
      color: white;
      border-radius: 25px;
      transition: background-color 0.3s ease;
    }

    .btn-custom:hover {
      background-color: rgb(0, 123, 255);
    }

    .form-control {
      border-radius: 5px;
      padding: 12px;
      font-size: 1rem;
    }

    .already-logined {
      text-align: center;
      margin-top: 20px;
      font-size: 1rem;
    }

    .already-logined a {
      color: rgb(0, 123, 255);
      text-decoration: none;
      font-weight: bold;
    }

    .already-logined a:hover {
      text-decoration: underline;
    }

    .logged-in-message {
      background-color: #28a745;
      color: white;
      padding: 15px;
      border-radius: 5px;
      margin-bottom: 20px;
      text-align: center;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }
  </style>
</head>

<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">
        <i class="fas fa-parking"></i> ParkScout
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto custom-navbar">
          <li class="nav-item text-center">
            <a class="nav-link" href="/">
              <div class="nav-icon">🏠</div>
              <div class="nav-text">Home</div>
            </a>
          </li>
          <li class="nav-item text-center">
            <a class="nav-link" href="/about">
              <div class="nav-icon">ℹ️</div>
              <div class="nav-text">About</div>
            </a>
          </li>
          <li class="nav-item text-center">
            <a class="nav-link" href="/services">
              <div class="nav-icon">🛠️</div>
              <div class="nav-text">Services</div>
            </a>
          </li>
          <li class="nav-item text-center">
            <a class="nav-link" href="/dashboard">
              <div class="nav-icon"><i class="fas fa-tachometer-alt"></i></div>
              <div class="nav-text">Dashboard</div>
            </a>
          </li>
          <li class="nav-item text-center">
            <a class="nav-link" href="/auth/login">
              <div class="nav-icon">🔑</div>
              <div class="nav-text">Login</div>
            </a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Logged-in Message (if applicable) -->
  <div id="loggedInMessage" class="logged-in-message" style="display: none;">
    <p>You are already logged in! <a href="dashboard.ejs">Go to your dashboard</a> or <a href="logout.ejs">Logout</a>.
    </p>
  </div>

  <!-- Login Form -->
  <div class="login-form">
    <h2 class="text-center">Login</h2>
    <form id="login-Form" method="post" action="/auth/login">
      <div class="mb-3">
        <label for="email" class="form-label">Email address</label>
        <input type="email" name="email" class="form-control" id="email" placeholder="Enter your email" required>
      </div>
      <div class="mb-3">
        <label for="password" class="form-label">Password</label>
        <input type="password" name="password" class="form-control" id="password" placeholder="Enter your password"
          required>
      </div>
      <button type="submit" class="btn btn-custom w-100">Login</button>
    </form>

    <div class="already-logined">
      <p>Don't have an account? <a href="/auth/register">Register here</a>.</p>
    </div>
  </div>

  <!-- <script>
    // Simulate checking if the user is already logged in
    const isLoggedIn = false; // Set this to `true` if the user is logged in

    if (isLoggedIn) {
      document.getElementById('loggedInMessage').style.display = 'block';
      document.querySelector('.registration-form').style.display = 'none';
    }

    // Registration form submission handling
    document.getElementById('registrationForm').addEventListener('submit', function (e) {
      e.preventDefault();
      // Add your registration logic here (e.g., validate user inputs, create account, etc.)
      alert("Registering...");  
      window.location.href = 'car_info.ejs'; // Redirect to the desired page after successful registration
    });
  </script> -->

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>