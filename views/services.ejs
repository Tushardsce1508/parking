<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ParkScout - Find Parking Availability</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Icons CDN -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
  <!-- Custom CSS -->
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background-color: rgb(13, 110, 253);

      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }

    .custom-navbar .nav-item {
      display: flex;
      flex-direction: column;
      align-items: center;
      margin: 0 15px;
    }

    .custom-navbar .nav-link {
      display: flex;
      flex-direction: column;
      align-items: center;
      color: white;
      text-decoration: none;
      font-size: 14px;
      padding: 10px 5px;
    }

    .custom-navbar .nav-icon {
      font-size: 20px;
      margin-bottom: 5px;
    }

    /* Grid Section */
    .grid-section {
      display: flex;
      justify-content: center;
      align-items: flex-end;
      /* Align the bottom of all grids */
      margin-top: 50px;
    }

    .grid-item {
      background-color: #ffffff;
      padding: 30px;
      border-radius: 8px;
      box-shadow: 0px 6px 15px rgba(0, 0, 0, 0.1);
      text-align: center;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      margin: 15px;
      flex: 1;
    }

    .grid-item:hover {
      transform: translateY(-10px);
      box-shadow: 0px 15px 30px rgba(0, 0, 0, 0.15);
    }

    /* Search Section */
    .search-section h2 {
      font-size: 2rem;
      margin-bottom: 30px;
    }

    .form-control {
      border-radius: 20px;
      margin-bottom: 15px;
    }

    .btn-search {
      background-color: #007bff;
      color: white;
      border-radius: 25px;
      padding: 10px 20px;
      font-weight: bold;
      width: 100%;
    }

    .btn-search:hover {
      background-color: #0056b3;
    }

    .result-message {
      margin-top: 30px;
      font-size: 1.2rem;
      font-weight: bold;
      color: #28a745;
    }

    .result-message.no-parking {
      color: #dc3545;
    }

    /* Payment Section */
    .payment-section h3 {
      font-size: 2rem;
      margin-bottom: 30px;
    }

    .payment-option {
      background-color: rgb(13, 110, 253);
      color: white;
      border-radius: 8px;
      padding: 20px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    .payment-option:hover {
      background-color: #0056b3;
    }

    .qr-code img {
      width: 150px;
      height: 150px;
    }

    .btn-receipt {
      background-color: #28a745;
      color: white;
      border-radius: 25px;
      padding: 10px 20px;
      font-weight: bold;
      width: 100%;
    }

    .btn-receipt:hover {
      background-color: #218838;
    }

    /* Footer */
    .footer {
      background-color: rgb(13, 110, 253);
      color: white;
      padding: 25px 0;
      text-align: center;
      margin-top: 50px;
    }

    .footer a {
      color: #ffcc00;
      text-decoration: none;
    }

    .footer a:hover {
      text-decoration: underline;
    }
  
    /* Responsive Design */
    @media (max-width: 768px) {
      .grid-item {
        margin-bottom: 30px;
      }

      .payment-section {
        padding: 20px 10px;
      }

      .payment-option {
        margin-bottom: 15px;
      }

      .search-section {
        padding: 30px 20px;
      }

      .result-message {
        font-size: 1rem;
      }
    }

    @media (max-width: 576px) {
      .payment-option {
        padding: 15px;
      }
    }

    .grid-section {
      flex-wrap: wrap;
    }
  </style>
</head>

<body>

  <!-- Navigation Bar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">
        <i class="fas fa-parking"></i> ParkScout
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto custom-navbar">
          <li class="nav-item text-center">
            <a class="nav-link" href="/">
              <div class="nav-icon">🏠</div>
              <div class="nav-text">Home</div>
            </a>
          </li>
          <li class="nav-item text-center">
            <a class="nav-link" href="/about">
              <div class="nav-icon">ℹ️</div>
              <div class="nav-text">About</div>
            </a>
          </li>
          <li class="nav-item text-center">
            <a class="nav-link" href="/services">
              <div class="nav-icon">🛠️</div>
              <div class="nav-text">Services</div>
            </a>
          </li>
          <li class="nav-item text-center">
            <a class="nav-link" href="/dashboard">
              <div class="nav-icon"><i class="fas fa-tachometer-alt"></i></div>
              <div class="nav-text">Dashboard</div>
            </a>
          </li>
          <li class="nav-item text-center">
            <a class="nav-link" href="/auth/login">
              <div class="nav-icon">🔑</div>
              <div class="nav-text">Login</div>
            </a>
          </li>
        </ul>
      </div>
    </div>
  </nav>


  <!-- Grid Section (Aligned Top and Bottom) -->
  <div class="container grid-section">
    <div class="grid-item search-section text-center">
      <h2>Search Parking Availability</h2>
      <form id="parking-search-form">
        <div class="form-group">

          <select id="location" class="form-control" required>
            <option value="">Select location</option>
            <option value="DSCE">DSCE</option>
            <option value="DSU">DSU</option>

          </select>
        </div>
        <div class="form-group">
          <select id="vehicle-type" class="form-control" required>
            <option value="">Select Vehicle Type</option>
            <option value="car">Car</option>
            <option value="bike">Bike</option>

          </select>
        </div>
        <button type="submit" class="btn btn-search">Check Availability</button>
      </form>

      <div id="result-message" class="result-message"></div>
    </div>
    
    

    <div class="grid-item payment-section text-center">
      <h2>Pay with QR Code <span>📱</span></h2>
      <input type="text" id="qr-input" class="form-control mb-2" placeholder="Enter your Valid Mobile number">
      <button id="generate-qr" class="btn btn-search">Generate QR Code</button>
      <img id="qr-code-image" style="display:none; margin:5px auto 0 auto; width:150px; height:150px;object-fit:contain;" />
      <a id="download-qr" href="#" download="qrcode.png" style="display:none; margin-top:5px;">

      
      <!-- <p class="scan-qr">Scan to pay</p> -->
      <button class="btn btn-receipt">Download QR Code</button>
      </a>
    </div>

    <script>
      const backendUrl = 'http://localhost:3000'; // Update to your deployed backend URL

      document.getElementById('generate-qr').onclick = function () {
        const value = document.getElementById('qr-input').value;
        if (!value) return alert('Enter payment info!');
        const qr = new QRious({ value, size: 200 });
        const img = document.getElementById('qr-code-image');
        img.src = qr.toDataURL();
        img.style.display = 'block';
        const dl = document.getElementById('download-qr');
        dl.href = qr.toDataURL();
        dl.style.display = 'inline-block';
      };

      // Process Card Payment
      document.getElementById('card-payment-form').addEventListener('submit', async (e) => {
        e.preventDefault();
        const response = await fetch(`${backendUrl}/pay-with-card`, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            amount: 20,
            currency: 'usd',
            source: 'tok_visa', // Replace with actual Stripe token
            description: 'Parking payment'
          })
        });
        const data = await response.json();
        if (data.message === 'Payment successful') {
          document.getElementById('download-receipt-card').style.display = 'block';
        } else {
          alert('Payment failed!');
        }
      });
    </script>

    <div class="grid-item payment-section text-center">
      <h3>Pay with Credit/Debit Card <span>💳</span></h3>
      <!-- <p>Enter your card details securely.</p> -->
      <form id="card-payment-form">
        <input type="text" id="card-number" class="form-control" placeholder="Card Number" required>
        <input type="text" id="card-cvc" class="form-control" placeholder="CVC" required>
        <input type="text" id="card-expiry" class="form-control" placeholder="MM/YY" required>
        <button type="submit" class="btn btn-search">Pay Now</button>
        <p style="margin-top:7px;">Your Details is secured with us</p>
      </form>
      <button id="download-receipt-card" class="btn btn-receipt" style="display:none;">Download Receipt 🧾</button>
      <button class="btn btn-receipt" onclick="downloadReceipt()">Download Receipt 🧾</button>
    </div>

  </div>
  </div>

  <!-- Footer Section -->
  <div class="footer">
    <p>&copy; 2024 <strong>ParkScout</strong>. All rights reserved.</p>
    <p><a href="#">Privacy Policy</a> | <a href="#">Terms & Conditions</a></p>
  </div>


  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>

  <!-- Qrious library for qrcode -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/qrious/4.0.2/qrious.min.js"></script>

  <!-- JavaScript for Parking Availability Check -->
  <script>
    // Simulating dynamic parking availability check
    document.getElementById('parking-search-form').addEventListener('submit', function (e) {
      e.preventDefault();

      var location = document.getElementById('location').value;
      var vehicleType = document.getElementById('vehicle-type').value;

      if (location && vehicleType) {
        // Here we simulate a check for parking availability (you can replace this with a real API call)
        var available = Math.random() > 0.5; // Randomly determine availability (50% chance)
        var resultMessage = document.getElementById('result-message');

        if (available) {
          resultMessage.textContent = ` Parking is available for ${vehicleType} in ${location}.`;
          resultMessage.classList.remove('no-parking');
          resultMessage.classList.add('available');
        } else {
          resultMessage.textContent = `Sorry, no parking available for ${vehicleType} in ${location}.`;
          resultMessage.classList.remove('available');
          resultMessage.classList.add('no-parking');
        }
      } else {
        alert('Please fill in all fields.');
      }
    });

    function downloadReceipt() {
      alert('Receipt downloaded!');
    }
  </script>
</body>